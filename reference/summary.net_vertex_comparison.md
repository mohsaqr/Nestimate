# Summarize a Two-Network Vertex-Bootstrap Comparison

Summarize a Two-Network Vertex-Bootstrap Comparison

## Usage

``` r
# S3 method for class 'net_vertex_comparison'
summary(object, ...)
```

## Arguments

- object:

  A `net_vertex_comparison` object.

- ...:

  Additional arguments (ignored).

## Value

The tidy summary data frame (one row per statistic).
