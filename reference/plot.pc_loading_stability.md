# Plot Composite-Weight Stability

Signed composite weights with bootstrap percentile intervals, faceted by
cluster.

## Usage

``` r
# S3 method for class 'pc_loading_stability'
plot(x, ...)
```

## Arguments

- x:

  A `pc_loading_stability` object.

- ...:

  Additional arguments (ignored).

## Value

A ggplot object.
