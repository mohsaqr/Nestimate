# Summarize a Vertex Bootstrap Result

Summarize a Vertex Bootstrap Result

## Usage

``` r
# S3 method for class 'net_vertex_bootstrap'
summary(object, ...)
```

## Arguments

- object:

  A `net_vertex_bootstrap` object.

- ...:

  Additional arguments (ignored).

## Value

The tidy summary data frame (one row per statistic).
