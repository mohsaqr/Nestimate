# Nestimate Project Status

## Current State: Initial Package Split

**Date:** 2026-03-14
**Version:** 0.1.0
**Origin:** Split from Saqrlab v0.3.0

## What Works

- Package loads cleanly (`devtools::load_all()`)
- NAMESPACE generated by roxygen2
- 2137 of 2138 tests pass (99.95%)
- All 7 estimators register on load
- All core functions operational: `build_network()`, `bootstrap_network()`, `permutation_test()`, `boot_glasso()`, `build_gimme()`, `build_mcml()`, `build_hon()`, `build_honem()`, `build_hypa()`, `mogen_transitions()`, `temporal_network()`, `velocity_tna()`

## Known Issues

1. **1 marginal test failure** in `test-mlvar.R:694` — person mean correlation test. Caused by simplified test data helper not matching Saqrlab's `simulate_data("mlvar")` statistical properties. Not a code bug.

2. **Test helpers** (`helper-simulate.R`) provide standalone `simulate_mtna()` and `simulate_data("mlvar")` that approximate Saqrlab's simulation functions. These are minimal — they generate structurally correct data but may not match exact statistical properties.

3. **Deprecated functions** still present: `estimate_network()` (wrapper → `build_network()`), `fit_network_model()`. Can be removed in a future version.

## File Inventory

| Category | Files | Tests |
|----------|-------|-------|
| Core estimation | 3 | 3 |
| Bootstrap/inference | 3 | 3 |
| GIMME | 1 | 1 |
| HON family | 4 | 4 |
| MCML | 1 | 2 |
| mlVAR | 1 | 1 |
| Temporal | 2 | 2 |
| Data utilities | 4 | 1 |
| Infrastructure | 4 | 0 |
| **Total** | **23** | **18** (including helper) |

## Relationship to Saqrlab

- Nestimate contains all network computation code
- Saqrlab retains simulation, testing, verification, and data resources
- Saqrlab will depend on Nestimate (add to Imports)
- Cross-dependency: Saqrlab's simulation/comparison functions call `build_network()` from Nestimate

## Next Steps

1. Add Nestimate to Saqrlab's DESCRIPTION Imports
2. Remove the 22 computation files from Saqrlab's R/
3. Update Saqrlab's NAMESPACE to import from Nestimate
4. Fix the 1 marginal mlvar test (improve test helper or skip)
5. Remove deprecated functions (estimate_network, fit_network_model)
6. Write README.md
7. Set up pkgdown site
8. Initialize git repository
