% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chain_structure.R
\name{plot.chain_structure}
\alias{plot.chain_structure}
\title{Plot method for \code{chain_structure}.}
\usage{
\method{plot}{chain_structure}(x, show_values = TRUE, digits = 2L, ...)
}
\arguments{
\item{x}{A \code{chain_structure} object.}

\item{show_values}{Logical. If \code{TRUE} (default), prints the numeric
probability inside each cell. Set \code{FALSE} for large state spaces
(n > 10) where labels overlap.}

\item{digits}{Integer. Decimal places for in-cell labels.}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
Renders the hitting-probability matrix as a heatmap, with rows and
columns ordered by communicating class so the block structure is
visible at a glance. State labels along both axes are coloured by
classification (absorbing / recurrent / transient). The subtitle
summarises the chain-level properties (regular, reversible).
}
\details{
Cell colour encodes \verb{P(ever reach j | start at i)}. The diagonal
uses the return-time convention (\verb{P(return to j in >= 1 steps)}),
matching \code{markovchain::hittingProbabilities}. A non-irreducible chain
shows zero off-block entries — visual evidence of one-way doors
between behavioural phases. An absorbing chain shows a column of 1's
for the absorbing state.
}
