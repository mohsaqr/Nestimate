% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlvar.R
\name{summary.net_mlvar}
\alias{summary.net_mlvar}
\title{Summary method for net_mlvar}
\usage{
\method{summary}{net_mlvar}(object, ...)
}
\arguments{
\item{object}{A \code{net_mlvar} object returned by \code{\link[=build_mlvar]{build_mlvar()}}.}

\item{...}{Unused; present for S3 consistency.}
}
\value{
The tidy coefficient \code{data.frame} - the same table \code{\link[=coefs]{coefs()}}
returns, with one row per \verb{(outcome, predictor)} pair and columns
\code{outcome}, \code{predictor}, \code{beta}, \code{se}, \code{t}, \code{p}, \code{ci_lower}, \code{ci_upper},
\code{significant}. Returned visibly, so calling \code{summary(fit)} at the
console prints the matrices and then the table.
}
\description{
Prints the three weight matrices and the significant temporal edges, then
returns the tidy coefficient table.
}
\examples{
\donttest{
# A three-variable ESM panel: 20 people x 20 beeps. `tired` is driven by
# `happy` one beep earlier, so the temporal network should recover it.
if (requireNamespace("lme4", quietly = TRUE)) {
  set.seed(1)
  n_beep <- 20
  ar1 <- function(n, phi) as.numeric(stats::filter(stats::rnorm(n), phi,
                                                   method = "recursive"))
  panel <- do.call(rbind, lapply(seq_len(20), function(i) {
    happy <- ar1(n_beep, 0.4)
    data.frame(
      id    = i,
      beep  = seq_len(n_beep),
      happy = happy + stats::rnorm(1),
      calm  = ar1(n_beep, 0.3) + stats::rnorm(1),
      tired = 0.5 * c(0, happy[-n_beep]) + stats::rnorm(n_beep) +
              stats::rnorm(1)
    )
  }))
  fit <- build_mlvar(panel, vars = c("happy", "calm", "tired"),
                     id = "id", beep = "beep")
  fit
  coefs(fit)
  summary(fit)
}
}

}
